imports:
    - { resource: config.yml }

doctrine:
    dbal:
        driver:   pdo_mysql
        host:     localhost
        port:     null
        dbname:   dodump
        user:     root
        password:
        charset:  UTF8