imports:
    - { resource: config.yml }

doctrine:
    dbal:
        driver:   pdo_mysql
        host:     localhost
        port:     null
        dbname:   dodump
        user:     root
        password: root
        charset:  UTF8